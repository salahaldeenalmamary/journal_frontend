import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
const sliceName = "UserMainMenu";
const slice = createSlice({
  name: sliceName,
  initialState,
  reducers: {},
});

export const {} = slice.actions;

export default slice.reducer;
